# Supabase seed baseline

No seed data is required for the Phase 9 release-candidate snapshot.

This directory exists to establish the expected Supabase project structure.
Add deterministic seed SQL files here when persistence tables are introduced.
